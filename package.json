{
  "name": "la-bonne-poire",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "shared",
    "backend",
    "mobile"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:backend": "npm run build -w shared && npm run dev -w backend",
    "dev:mobile": "npm run build -w shared && npm run start -w mobile",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:migrate": "npm run db:migrate -w backend",
    "db:seed": "npm run db:seed -w backend",
    "db:studio": "npm run db:studio -w backend",
    "test": "npm run test -w backend",
    "build": "npm run build -w shared && npm run build -w backend",
    "typecheck": "npm run build -w shared && npm run typecheck -w backend"
  }
}
